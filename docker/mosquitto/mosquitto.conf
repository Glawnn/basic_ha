# Mosquitto dev — miroir prod HA OS (pc-ha)
# Dual auth: anonymes OK + password_file si credentials fournis
# Dev password = test/test (local only, jamais prod)
listener 1883
protocol mqtt
allow_anonymous true
password_file /mosquitto/config/passwd

persistence false
log_dest stdout
log_type all
connection_messages true
